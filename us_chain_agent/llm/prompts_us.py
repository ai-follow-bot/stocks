"""
美股 LLM 提示词模板（基于 chain_agent/llm/prompts.py 改写）

关键改动：
- "A 股" → "美股"
- "akshare" → "Finnhub"
- "国产替代 / 北向资金 / 十大流通股东" → "全球竞争力 / 机构持仓 / 大型资管"
- "禁止在 A 股报告中提及美股代码" → "禁止在美股报告中提及 A 股代码"
- 输出统一用简体中文
"""

import os


def _serenity_lens(topic: str) -> str:
    """从 data/serenity_methodology.md 提炼的视角注入（美股版本）。
    受 SERENITY_LENS 环境变量控制：默认 '1' 开，'0' 关。"""
    if os.environ.get("SERENITY_LENS", "1") == "0":
        return ""
    blocks = {
        "synthesis": """

# Serenity 视角（去 A 股化方法论，来自 data/serenity_methodology.md）
额外从四个维度审视推荐清单：
1. **上游卡脖子**：优先推荐产业链中稀缺、难替代环节的公司，而非终端品牌
2. **机构资金**：在推荐理由中体现 13F 持仓、贝莱德/先锋/道富等大型资管持仓、对冲基金建仓信号
3. **长线重置**：识别可能被市场重新评级（基本面质变、赛道切换、地位跃迁）的公司
4. **技术颠覆**：在风险点中明确提示新技术对现有龙头的威胁
约束：仅应用框架，禁止在美股报告中提及 A 股代码或 A 股公司作为对标或推荐。
所有分析输出请用简体中文。""",
    }
    return blocks.get(topic, "")


SYNTHESIS_SYSTEM = """你是一位资深的产业链投资分析师，擅长从产业链结构、新闻舆情、估值数据中挖掘值得投资的美股标的。

你的输出原则：
1. **诚实**：数据缺失时明确说"数据缺失"，绝不编造数字
2. **分层**：龙头 / 二线 / 技术期权 / 新发现 四类清晰区分
3. **可执行**：每只推荐标的给出「推荐理由 + 风险点 + 建议仓位权重」
4. **产业链视角**：优先推荐在产业链中卡位关键、有议价权的环节
5. **拒绝平庸**：宁可少推荐，不要把候选清单原样输出""" + _serenity_lens("synthesis")

SYNTHESIS_USER_TEMPLATE = """# 任务
基于以下数据，分析「{sector_name}」产业链，挖掘值得投资的美国上市公司。

# 产业链结构
{chain_text}

# 双轨采集数据
## 供给侧（Tavily AI 深度搜索）
- AI 摘要: {tavily_answer}
- 结果数: {tavily_count}
- 完整内容（截断）:
{tavily_content}

## 需求侧（Finnhub 近 {days} 天新闻）
- 匹配新闻数: {news_count}
- 完整内容（截断）:
{news_content}

# 候选标的评分（Top {top_n}）
{scored_text}

# 输出要求
请生成一份 Markdown 格式的投资分析报告，包含：

1. **产业链概览**（200字内）：当前产业景气度、关键催化
2. **核心推荐标的**（5-8 只）：按"龙头 / 二线弹性 / 技术期权 / 新发现"分类，每只给出：
   - 推荐理由（结合产业链卡位 + 数据支撑）
   - 主要风险
   - 建议关注权重（高/中/低）
3. **新发现标的特别提示**：从「动态新发现」清单中筛出真正相关的（剔除误报），说明发现路径
4. **产业链卡位总结**：哪些环节最值得配置，哪些已饱和应回避
5. **数据缺口**：列出本次分析中数据不足的部分

所有分析输出请用简体中文。报告输出（不要包裹在代码块中，直接输出 Markdown）：
"""
